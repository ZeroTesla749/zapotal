<?xml version="1.0" encoding="utf-8"?>
<!--
  ================================================================
  ANDROIDMANIFEST.XML
  ================================================================
  Declara permisos, actividades y servicios de la app.
  ► El único permiso que necesitamos es INTERNET para sincronizar
    con Google Sheets. Sin internet la app funciona igual (offline).
-->
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Permiso de internet — solo para sincronización con Google Sheets -->
    <uses-permission android:name="android.permission.INTERNET" />
    <!-- Estado de red — para saber si hay conexión antes de sincronizar -->
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:name=".LagunaApp"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.LagunaZapotal"
        android:networkSecurityConfig="@xml/network_security_config">

        <!-- ── ACTIVIDAD PRINCIPAL ── -->
        <!-- Es la única Activity — la navegación se hace con Fragments -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

    </application>

</manifest>
